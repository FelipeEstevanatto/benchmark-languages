# Take the information from the sorting .txt inside Results, average every 3 values and plot them
# The x-axis is the number of elements in the array, the y-axis is the time taken to sort the array
# The plot is saved as a .png file in the Results folder

import matplotlib.pyplot as plt
